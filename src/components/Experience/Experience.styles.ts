import { createStyles } from "../../interfaces/create_styles";

export const experienceStyles = createStyles({
    gridContainer: {
        margin:"16px 8px "
    },
    textR:{
        display: "flex",
        justifyContent: "flex-end"
    },
    textC:{
        display: "flex",
        justifyContent: "center"
    },
    textL:{
        display: "flex",
        justifyContent: "flex-start"
    },
    ul:{
        marginTop:0
    }
});
